import Link from 'next/link';
import { prisma } from '@/lib/prisma';
export default async function SubscribersPage() { const subscribers = await prisma.newsletterSubscriber.findMany({ orderBy: { createdAt: 'desc' }, take: 100 }); return <><div className="flex justify-between"><h1 className="text-3xl font-bold">Subscribers</h1><Link className="underline" href="/api/export/subscribers">Export XLSX</Link></div><div className="mt-6 overflow-x-auto rounded-xl bg-white"><table className="w-full text-left text-sm"><thead><tr>{['Name','Email','Type','Status','Source','Subscribed'].map((h) => <th className="p-3" key={h}>{h}</th>)}</tr></thead><tbody>{subscribers.map((s) => <tr className="border-t" key={s.id}><td className="p-3">{s.name ?? '—'}</td><td className="p-3">{s.email}</td><td className="p-3">{s.subscriptionType}</td><td className="p-3">{s.status}</td><td className="p-3">{s.source ?? '—'}</td><td className="p-3">{s.subscribedAt.toLocaleDateString()}</td></tr>)}</tbody></table></div></>; }
