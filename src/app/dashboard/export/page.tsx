export default function ExportPage() { return <><h1 className="text-3xl font-bold">Export data</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{['leads','subscribers'].map((kind) => <section className="rounded-xl bg-white p-5" key={kind}><h2 className="font-bold capitalize">{kind}</h2><p className="my-3 text-sm text-stone-600">Download the current database records.</p><div className="flex gap-3"><a className="underline" href={`/api/export/${kind}`}>XLSX</a><a className="underline" href={`/api/export/${kind}?format=csv`}>CSV</a></div></section>)}</div></>; }
