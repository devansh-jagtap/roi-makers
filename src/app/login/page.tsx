'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { createSupabaseBrowserClient } from '@/lib/supabaseClient';
export default function LoginPage() { const [email, setEmail] = useState(''), [password, setPassword] = useState(''), [error, setError] = useState(''), [loading, setLoading] = useState(false); const router = useRouter(); async function login(event: FormEvent) { event.preventDefault(); setLoading(true); setError(''); const { error: authError } = await createSupabaseBrowserClient().auth.signInWithPassword({ email, password }); if (authError) setError('Invalid email or password.'); else router.replace('/dashboard'); setLoading(false); } return <main className="min-h-screen grid place-items-center bg-[#060010] p-6"><form onSubmit={login} className="w-full max-w-md rounded-3xl bg-white p-8 space-y-5"><h1 className="text-3xl font-bold">ROI Makers Dashboard</h1><label className="block">Email<input className="mt-1 w-full border rounded-lg p-3" type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></label><label className="block">Password<input className="mt-1 w-full border rounded-lg p-3" type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /></label>{error && <p className="text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-[#060010] p-3 text-white">{loading ? 'Signing in…' : 'Sign in'}</button></form></main>; }
